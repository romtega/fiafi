<!DOCTYPE html>
<html>
<head>
	<title>FiaFi</title>
	<%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
	<%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
	<%= csrf_meta_tags %>

	<script type="text/javascript" src="https://maps.googleapis.com/maps/api/js?key=AIzaSyAjuwqrRgNcVHO6ph7CMcBWPwAOZhawq7U"></script>

</head>
<body>
	<%= render "layouts/header" %>

	<!--  asi estan originalmente los flash messages, afuera del container y el landing class
	
	<% flash.each do |type, message| %>
	<div class="alert alert-success fade-in">
		<button class="close" data-dismiss="alert">x</button>
		<%= message %>
	</div>
	<% end %>
-->
	<!--  si quiero que nada mas aparesca en landing page, quito los comentarios

	<% if current_page?(root_path) %>
	<div class="landing">
		<% else %>
	<div class="container">
		<% end %> 
	-->
	<div class="landing">
		<% flash.each do |type, message| %>
		<div class="alert alert-success fade-in">
			<button class="close" data-dismiss="alert">x</button>
			<%= message %>
		</div>
		<% end %>

		<div class="container">

			<%= yield %>
		</div>
	</div>
	</body>
	</html>